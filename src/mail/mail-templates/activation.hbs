<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ title }}</title>
    <style>
      body {
        font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
        font-size: 1rem;
        line-height: 1.5;
        color: #212529;
        background-color: #f5f5f5;
        text-align: left;
      }
      .buttonContainer {
        width: 100%;
      }
      .container {
        width: 90%; /* adjust to your desired width */
        margin: 0 auto;
        background-color: #fff;
        padding: 1rem;
        border-radius: 0.25rem;
      }
      .sub {
        font-size: 0.8rem;
        color: #718096;
      }
      .button {
        padding: 1.2rem 3rem;
        font-size: medium;
        background-color: #63b3ed;
        border-radius: 0.5rem;
        color: #fff;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h3>Registration Confirmation</h3>
      <p>Hi {{ name }},</p>
      <p>
        Thank you for registering for a Savy account with email address
        {{ email }}. You're almost there!
      </p>
      <p>
        Please enter the following 6-digit token to confirm your email address:
      </p>
      <div class="button">{{ token }}</div>
      <p class="sub">This token will expire in 24 hours.</p>
    </div>
  </body>
</html>